import { StyleSheet } from "react-native";

const AppStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#a68f8fff',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 20,
    }
})

export default AppStyle;